#include<bits/stdc++.h>
using namespace std;

void solve(vector<int>&a,const int n) {
    return;
}

int main(){
    int t;
    cin >> t;
    
    while (t--) {
        int n;
        cin>>n;
        vector<int> a(n);
        for(int i = 0; i<n; i++){
            cin>>a[i];
        }
        solve(a,n);
    }
    return 0;
}
